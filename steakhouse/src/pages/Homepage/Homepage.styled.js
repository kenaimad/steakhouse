import styled from 'styled-components';
import { Colors } from '../../utils/palette';

export const Container = styled.div`
  margin-top: 70px;
  background-image: linear-gradient(${Colors.primaryBackground}, white, white, white);
`